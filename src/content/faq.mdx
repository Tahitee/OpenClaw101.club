---
title: "常见问题 FAQ"
description: "OpenClaw 常见问题解答：覆盖费用、安装、渠道接入、模型配置、安全等高频问题，快速解决你的疑惑。"
---

# ❓ 常见问题 FAQ

这里整理了使用 OpenClaw 过程中最常遇到的问题。如果你的问题不在列表中，欢迎在评论区提问。

---

## 🟢 入门问题

### Q：OpenClaw 是免费的吗？

**A：OpenClaw 本身完全免费开源（MIT 协议）。** 但你需要为以下部分付费：

- **AI 模型**：需要 Anthropic / OpenAI / Google 等的 API Key 或订阅（推荐 Anthropic Claude Pro/Max）
- **服务器**：如果你要 24 小时在线，需要一台 VPS（最低约 $5/月）
- **可选服务**：ElevenLabs（语音）、Brave Search API（搜索）等

### Q：我需要什么技术基础？

**A：最低要求：**

- 会使用终端/命令行（基本的 `cd`、`ls`、`npm` 命令）
- 了解什么是 Node.js（不需要会写代码）
- 有一台能安装软件的电脑（macOS / Linux / Windows WSL2）

**不需要：**
- 不需要会编程
- 不需要了解 AI 原理
- 不需要运维经验（虽然有的话更好）

### Q：支持 Windows 吗？

**A：支持，但强烈建议使用 WSL2（Windows Subsystem for Linux）。** 原生 Windows 支持有限，WSL2 环境下体验与 Linux 一致。

### Q：需要买 Mac Mini 吗？

**A：不需要。** OpenClaw 可以运行在任何有 Node.js 22+ 的系统上。Mac Mini 只在你需要以下功能时才有必要：
- iMessage 集成（需要 macOS）
- macOS 菜单栏应用
- Talk Mode 语音功能（macOS 体验最佳）

大多数用户用一台 Linux VPS 就够了。

### Q：可以用手机操作吗？

**A：OpenClaw 本身需要运行在电脑/服务器上，但你可以通过手机上的聊天 App（WhatsApp、Telegram、飞书等）与它交互。** 另外，iOS 和 Android 都有 OpenClaw Node 伴侣应用，可以将手机作为远程设备节点。

---

## 🔧 安装问题

### Q：安装卡在 "wake up my friend" / onboarding 不动了怎么办？

**A：几种可能的原因：**

1. **网络问题**：模型 API 无法访问，检查你的 API Key 和网络
2. **API Key 无效**：确认 Key 是否正确、是否有余额
3. **Node 版本过低**：必须 Node 22+，运行 `node -v` 检查

尝试：
```bash
openclaw doctor        # 诊断问题
openclaw logs --follow # 查看实时日志
```

### Q：npm install 报错 / 权限问题？

**A：常见解决方案：**

```bash
# 避免用 sudo 安装全局包，改用 nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.0/install.sh | bash
nvm install 22
nvm use 22
npm install -g openclaw@latest
```

### Q：Docker 部署后无法访问 Control UI？

**A：检查以下几点：**

1. 端口映射是否正确：`-p 127.0.0.1:18789:18789`
2. 如果是远程服务器，需要 SSH 隧道：`ssh -N -L 18789:127.0.0.1:18789 user@server`
3. 检查 Gateway token 是否正确
4. 运行 `docker compose logs openclaw-gateway` 查看日志

---

## 📱 渠道问题

### Q：WhatsApp 扫码后频繁掉线？

**A：WhatsApp Web 连接确实不太稳定，几个建议：**

- 确保服务器网络稳定
- 不要在手机上退出 WhatsApp
- 设置 `web.reconnect` 自动重连
- 考虑使用独立的手机号
- 运行 `openclaw doctor` 检查连接状态

### Q：Telegram Bot 在群里不回复？

**A：检查以下配置：**

1. Bot 是否已添加到群组
2. 群组隐私模式：在 BotFather 中用 `/setprivacy` 设为 Disabled
3. `requireMention` 是否开启：如果是，需要 @bot 才会回复
4. `allowFrom` 是否包含了发送者

### Q：QQ 机器人不能发链接？

**A：这是 QQ 平台的限制，不是 OpenClaw 的问题。** QQ 机器人平台对消息内容有严格审核，包括：
- 不能发送 URL
- 不能发送某些敏感关键词
- 有消息频率限制

建议在 QQ 渠道使用时，配置 AI 不要在回复中包含链接。

### Q：飞书/钉钉提示权限不足？

**A：确认以下事项：**

1. 应用已发布（不是草稿状态）
2. 所需权限已申请并审批
3. 事件订阅已正确配置
4. 如果是企业应用，确认可见范围包含了目标用户

---

## 🧠 使用问题

### Q：AI 的记忆怎么工作？会不会忘记之前的对话？

**A：OpenClaw 有两层记忆机制：**

1. **会话内记忆**：当前对话的上下文，关闭后根据 reset 策略可能清除
2. **持久记忆**：MEMORY.md 和 memory/YYYY-MM-DD.md 文件，永久保存

如果 AI 忘记了重要信息，你可以让它"记住"——它会写入记忆文件。

### Q：怎么切换 AI 模型？

**A：在对话中直接发送：**

```
/model              # 查看可用模型列表
/model 3            # 选择编号 3 的模型
/model anthropic/claude-opus-4-6  # 指定具体模型
```

### Q：怎么重置对话？

**A：发送以下命令：**

```
/new          # 开始新对话
/reset        # 重置当前会话
/new opus     # 新对话并切换到 Opus 模型
```

### Q：多个人可以共用一个 OpenClaw 吗？

**A：可以，但需要正确配置：**

1. 设置 `session.dmScope: "per-channel-peer"` 隔离每个人的会话
2. 使用 pairing 或 allowlist 控制谁能访问
3. 考虑多 Agent 方案，给每个人分配独立的 AI 人格

⚠️ **重要**：不配置会话隔离的话，所有人共享同一个对话上下文，会泄露隐私！

---

## 💰 费用问题

### Q：每月大概花多少钱？

**A：典型场景估算：**

| 用途 | 月费用估计 |
|------|-----------|
| AI 模型（Anthropic API，轻度使用） | $5-20 |
| AI 模型（Claude Pro 订阅） | $20/月 |
| AI 模型（Claude Max 订阅） | $100-200/月 |
| VPS（Hetzner/腾讯云最低配） | $3-5 |
| 域名 | $1/月（年付） |
| ElevenLabs 语音（可选） | $5-22 |
| **总计（最低配置）** | **约 $8-25/月** |

使用 Claude Pro/Max 订阅 + OAuth 认证是性价比最高的方案。

### Q：有没有完全免费的方案？

**A：接近免费的方案：**

- 模型用 Google Gemini（有免费额度）
- 服务器用自己的电脑（不需要 VPS）
- 渠道用 Telegram（免费 Bot）

但体验会比付费方案差一些。

---

## 🔒 安全问题

### Q：我的聊天数据安全吗？

**A：OpenClaw 是完全自托管的，数据只存在你自己的服务器上。** 但需要注意：

- 对话会通过 AI 提供商的 API 发送（Anthropic/OpenAI 等有数据政策）
- 使用 OAuth 订阅比 API Key 在隐私上更优
- 确保服务器本身的安全（SSH 密钥、防火墙等）

### Q：别人能入侵我的 AI 助手吗？

**A：做好以下防护就很安全：**

1. 设置 Gateway 认证（token 或 password）
2. 绑定 loopback，不暴露到公网
3. 使用 DM pairing 或 allowlist
4. 运行 `openclaw security audit` 定期检查
5. 不要把 AI 助手暴露给不信任的人

---

## 🆘 还有问题？

- **官方文档**：docs.openclaw.ai
- **Discord 社区**：discord.gg/clawd
- **GitHub Issues**：github.com/openclaw/openclaw/issues
- **本站评论区**：每篇文章底部都可以留言

---

> 💬 **有问题或建议？** 欢迎在下方评论区留言讨论。
